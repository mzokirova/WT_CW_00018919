doctype html
html
  head
    title Edit Review
    link(rel="stylesheet", href="/styles/style.css")
  body
    .container2
      h1 Edit Review
      form.edit_review(action=`/editReview/${review.id}` method="POST")
        label(for="rate") ⭐ Rating:
        input(type='number' name='rate' min="1" max="5" value=review.rate required)

        label(for='message') 📝 Message:
        textarea(name="message" required)= review.message 

        button.btn.btn-success(type="submit") Save Changes
      a.btn.btn-danger(id = "cancel_editrev" href="/reviews") Cancel